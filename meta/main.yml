galaxy_info:
  author: Florian Kempenich
  description: Install Docker on Ubuntu. And optionally `docker-compose` and `docker-machine`.

  license: MIT

  min_ansible_version: 1.2

  platforms:
    - name: Ubuntu
      versions:
        - all

  galaxy_tags: 
    - docker
    - docker-compose
    - docker-machine
    - devops
    - container

dependencies: []